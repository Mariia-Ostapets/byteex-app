import Logo from "../Logo/Logo";
import Layout from "../Layout/Layout";

export default function Header() {
  return (
    <Layout>
      <header>
        <Logo />
      </header>
    </Layout>
  );
}
